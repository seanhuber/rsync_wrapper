require 'spec_helper'

describe RsyncWrapper do
  it 'has a version number' do
    expect(RsyncWrapper::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
